apiVersion: v1
kind: Pod
metadata:
  name: zipkin
spec:
  selector:
    app: zipkin
  containers:
    - image: openzipkin/zipkin
      name: zipkin
      ports:
        - containerPort: 9411
          name: zipkinhttpport
          protocol: TCP